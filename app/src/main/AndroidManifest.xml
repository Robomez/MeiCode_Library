<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MeiCode_Library"
        tools:targetApi="31">
        <activity
            android:name=".WebsiteActivity"
            android:exported="false" />
        <activity
            android:name=".FavBookActivity"
            android:exported="false" />
        <activity
            android:name=".ReadingBookActivity"
            android:exported="false" />
        <activity
            android:name=".WishBookActivity"
            android:exported="false" />
        <activity
            android:name=".AlrReadBookActivity"
            android:exported="false"
            android:label="@string/activity_already_read"/>
        <activity
            android:name=".BookActivity"
            android:exported="false" />
        <activity
            android:name=".AllBooksActivity"
            android:exported="false"
            android:label="@string/activity_all_books"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="My library">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>